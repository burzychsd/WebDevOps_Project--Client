import React, { Fragment } from 'react';

const Home = (props) => {
    return (
        <Fragment>
        	<h1>Home</h1>
        </Fragment>
    );
};

Home.displayName = 'Home';

export default Home;
