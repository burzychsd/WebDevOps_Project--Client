import React, { Fragment } from 'react';

const Login = (props) => {
    return (
        <Fragment>
        	<h1>Login</h1>
        </Fragment>
    );
};

Login.displayName = 'Login';

export default Login;